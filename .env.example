# AI Orchestrator Framework - Environment Variables

# ============================================
# LLM 설정
# ============================================

# LLM 인증 방식 (api-key 또는 cli)
# - api-key: Anthropic API 키 사용 (유료, https://console.anthropic.com)
# - cli: 로컬 Claude CLI 사용 (Max Plan 구독 필요)
LLM_AUTH_METHOD=api-key

# === API 키 방식 (LLM_AUTH_METHOD=api-key) ===
# Anthropic API Key
# https://console.anthropic.com/account/keys 에서 발급
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# === CLI 방식 (LLM_AUTH_METHOD=cli) ===
# Claude CLI 경로 (기본값: claude)
# Max Plan 구독 후 로컬에 Claude 설치 필요
# CLAUDE_CLI_PATH=claude

# ============================================
# 환경 설정
# ============================================

# 실행 환경 (development, production, test)
NODE_ENV=development

# ============================================
# Git 설정 (선택)
# ============================================

# Git 사용자 정보 (커밋 시 사용)
# GIT_USER_NAME=Your Name
# GIT_USER_EMAIL=your.email@example.com

# ============================================
# 고급 설정 (선택)
# ============================================

# 프롬프트 디렉토리 경로 (기본값: ./prompts)
# PROMPTS_DIR=./prompts

# 로그 레벨 (debug, info, warn, error)
# LOG_LEVEL=info

# Self-healing 활성화 여부 (true, false)
# SELF_HEALING_ENABLED=true

# Self-healing 최대 재시도 횟수 (1-10)
# SELF_HEALING_MAX_ATTEMPTS=3

# Self-healing 타임아웃 (ms)
# SELF_HEALING_TIMEOUT=300000

# ============================================
# 사용 방법
# ============================================
# 1. 이 파일을 .env로 복사:
#    cp .env.example .env
#
# 2. ANTHROPIC_API_KEY를 실제 키로 변경
#
# 3. 필요한 설정 조정
#
# ⚠️  주의: .env 파일은 절대 커밋하지 마세요!
#    (.gitignore에 이미 추가되어 있습니다)
